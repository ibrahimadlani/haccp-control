apiVersion: 1

providers:
  - name: HACCP Dashboards
    orgId: 1
    folder: HACCP Control
    folderUid: haccp
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    allowUiUpdates: false
    options:
      path: /etc/grafana/provisioning/dashboards
